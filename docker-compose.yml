services:
  backend:
    build:
      context: ./backend
    container_name: rest-api-sheet
    restart: always
    ports:
      - "8001:8000"
    env_file:
      - ./backend/.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
